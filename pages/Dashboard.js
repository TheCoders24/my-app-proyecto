"use client";

const { requestToBodyStream } = require("next/dist/server/body-streams");

const DashboardPage = () => {
    return(
        <h1>DashboardPage</h1>
    );
};

export default DashboardPage;   